export default function HomePage() {
  return (
    <main className="page-shell">
      <section className="intro">
        <h1>AccessFlow</h1>
        <p>
          AccessFlow is a workflow case study for clinical study workspace
          access requests. This scaffold establishes the typed web/API
          foundation while the request workflow UI remains intentionally
          deferred.
        </p>
      </section>
    </main>
  );
}
